# 🗺️ LangGraph 學習地圖 (Learning Map)

**角色定位:** LangGraph 是基於 LangChain 生態系統的庫，專門用於構建複雜、有狀態（Stateful）且具備循環（Cyclical）的 LLM 代理（Agent）工作流。它讓開發者能夠將 LLM 的執行流程視為一個有向圖（Directed Graph）。

**目標受眾:** 具備 Python 基礎，了解 LangChain 基本概念，並希望從簡單的鏈式呼叫（Chains）過渡到複雜、多步驟決策流程的開發者。

---

## 🚀 階段一：基礎奠定與概念掌握 (Foundation & Concepts)

**目標:** 理解圖論在 LLM 流程中的應用，掌握 LangGraph 的核心組件。

### 📚 學習重點
1.  **LangChain 複習:** 確保理解 `LCEL` (LangChain Expression Language) 的基本用法。
2.  **圖論基礎:** 了解什麼是節點 (Nodes)、邊 (Edges)、狀態 (State) 和循環 (Cycles)。
3.  **LangGraph 核心概念:**
    *   **State:** 流程中所有步驟共享和更新的資料結構（通常是字典或 Pydantic 模型）。
    *   **Node:** 圖中的一個單元操作，代表一個具體的步驟（例如：呼叫 LLM、執行工具、資料處理）。
    *   **Graph:** 包含所有節點和定義了它們之間轉換邏輯的結構。
    *   **Edges/Conditional Edges:** 定義了當前節點執行完畢後，流程應該轉向哪個下一個節點（這是實現決策邏輯的關鍵）。

### 🛠️ 實作任務
*   **任務 1:** 建立一個最簡單的「Hello World」圖：輸入 -> 節點 A (打印訊息) -> 結束。
*   **任務 2:** 實作一個簡單的狀態更新流程：輸入一個名字，在圖中經過兩個節點，最終的狀態包含「原始名字」和「處理後的問候語」。

---

## ⚙️ 階段二：核心功能實作與代理構建 (Core Implementation & Agents)

**目標:** 能夠構建具備決策能力、能使用工具的複雜代理。

### 📚 學習重點
1.  **工具呼叫 (Tool Calling):** 如何將外部工具（如搜尋 API、資料庫查詢）封裝成一個可被圖結構調用的節點。
2.  **條件路由 (Conditional Edges):** 這是 LangGraph 最強大的部分。學習如何根據當前狀態或 LLM 的輸出，決定下一步的執行路徑（例如：如果 LLM 判斷需要工具，則轉到 `Tool_Node`；否則，轉到 `Final_Answer_Node`）。
3.  **狀態管理 (State Management):** 深入理解如何定義和更新共享的 `State`，確保資料在多個節點間傳遞的原子性和一致性。
4.  **循環邏輯 (Cycles):** 實作需要多次迭代的流程，例如：Agent 思考 -> 執行工具 -> 觀察結果 -> 再次思考，直到達到收斂條件。

### 🛠️ 實作任務
*   **任務 3 (基礎 Agent):** 構建一個能使用單一工具（例如：天氣查詢）的代理。流程：`Input` -> `LLM_Router` (決定是否需要工具) -> `Tool_Node` -> `Observation_Node` -> `Final_Answer`。
*   **任務 4 (複雜 Agent):** 構建一個能處理多步驟、多工具的代理（例如：一個「旅行規劃師」Agent，需要先查詢天氣，再查詢景點，最後生成行程）。

---

## 🧠 階段三：進階優化與生產級應用 (Advanced & Production Readiness)

**目標:** 處理記憶、優化性能，並將模型部署到實際環境。

### 📚 學習重點
1.  **記憶機制 (Memory):** 如何在圖結構中整合記憶。這通常涉及在 `State` 中加入歷史記錄，並在每個節點開始時讀取和更新它。
2.  **並行執行 (Parallelism):** 了解如何設計圖結構，讓多個節點可以同時執行（例如：同時查詢天氣和股票價格）。
3.  **錯誤處理與重試 (Error Handling):** 在圖的邊緣定義錯誤捕獲和重試的邏輯，確保流程的健壯性。
4.  **性能優化:** 了解如何限制 LLM 的調用次數（Max Steps）和優化狀態的傳遞開銷。

### 🛠️ 實作任務
*   **任務 5 (記憶 Agent):** 構建一個能夠記住前幾輪對話內容，並根據歷史上下文進行回答的聊天機器人。
*   **任務 6 (生產級流程):** 模擬一個需要多個外部 API 呼叫、且需要處理 API 失敗和重試的業務流程。

---

## 🏆 學習總結與資源建議

| 階段 | 核心概念 | 關鍵技能 | 建議專案 |
| :--- | :--- | :--- | :--- |
| **I** | State, Node, Edge | 流程定義，基本組件串接 | 簡單的資料轉換流程 |
| **II** | Conditional Edges, Tools | 決策邏輯，外部互動 | 具備多步驟決策的 Agent |
| **III** | Memory, Parallelism | 流程優化，健壯性設計 | 複雜的業務流程模擬器 |

**💡 學習建議:**
1.  **從簡單開始:** 不要急著構建複雜的 Agent。先用 LangGraph 實現一個簡單的狀態機（State Machine）。
2.  **閱讀官方範例:** 官方文件中的範例是最好的教材，務必手動運行並理解每個 `add_node` 和 `add_edge` 的作用。
3.  **實戰為王:** 每次學習一個新概念，都必須立即用一個小專案來驗證它。